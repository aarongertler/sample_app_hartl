<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <!-- Note to self: commenting out ERB is hard! Avoid if possible -->
  <%= render @users %>
</ul>

<%= will_paginate %>